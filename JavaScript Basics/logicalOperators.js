// && - AND operator
// || - OR operator

let isVerified = true
let isLoggedin =true 
let hasPaymentToken = false
let isGuest = true

if(isVerified && isLoggedin && hasPaymentToken) { 
    console.log("Welcome to mycourse \n allowed access!")}
else{console.log("free preview")}
